"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const Executor_1 = __importDefault(require("../Executor"));
class ParallelExecutor extends Executor_1.default {
    constructor() {
        super(...arguments);
        this.parallel = true;
    }
    /**
     * Execute tasks in parallel.
     */
    async run(handler, tasks, value) {
        this.debug('Parallelizing %d tasks', tasks.length);
        return Promise.all(tasks.map(task => handler(task, value)));
    }
}
exports.default = ParallelExecutor;
