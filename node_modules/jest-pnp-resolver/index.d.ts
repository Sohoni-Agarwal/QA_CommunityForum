type JestResolverOptions = {
  basedir: string;
  defaultResolver: (self: string, opts: any) => string,
  extensions?: Array<string>,
};

export default function resolve(
  self: string,
  options: JestResolverOptions,
): string;
